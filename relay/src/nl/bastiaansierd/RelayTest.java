package nl.bastiaansierd;

public class RelayTest {
    public static void main( String[] args) {
        /*
        Thread arduinoThread = new Thread(new ArduinoToServerCommuncationHandler(MockArduinoConnection.getInstance(), ServerConnection.getInstance()));
        Thread relayThread = new Thread(new ServerToArduinoCommunicationHandler(MockArduinoConnection.getInstance(), ServerConnection.getInstance()));

        arduinoThread.start();
        relayThread.start();

         */
    }
}
